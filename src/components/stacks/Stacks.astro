---
import SectionHeader from '../SectionHeader.astro'
import StackList from './StackList.astro'

import { STACK } from '../../constants'
---

<section id="stack" class="min-h-screen flex flex-col justify-center items-center py-20 px-4">
    <SectionHeader title={STACK.title} description={STACK.description} />
    <StackList />
</section>
